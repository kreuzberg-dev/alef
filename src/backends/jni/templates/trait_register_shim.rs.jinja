#[unsafe(no_mangle)]
pub unsafe extern "system" fn {{ symbol }}(
    mut env: EnvUnowned,
    _class: JClass,
    _impl_obj: JObject,
{% if not has_super_trait %}
    _name: JString,
{% endif %}
) {
    // SAFETY: env is a valid EnvUnowned passed by the JVM for this native call frame.
    let mut __jni_attach_guard = unsafe { jni::AttachGuard::from_unowned(env.as_raw()) };
    let env = __jni_attach_guard.borrow_env_mut();

    // TODO: Trait bridge registration stubs
    // This is a placeholder that accepts the trait impl from Kotlin but does not yet
    // forward it to the Rust registry. Full implementation requires generating
    // trait adapters that marshal method calls through JNI.
    //
    // For now, this prevents UnsatisfiedLinkError when Kotlin tries to call this function,
    // allowing tests that only use registry operations (list, clear) to proceed.
    // Tests that actually register and call trait methods will fail at runtime with
    // appropriate error messages.

    // Silently succeed for now — the Kotlin test can proceed even without actual registration.
}
