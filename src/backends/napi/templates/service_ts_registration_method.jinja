  /**
{% if doc %}
   * {{ doc }}
{% endif %}
   */
  {{ method_name }}({{ meta_sig }}): (fn: (...args: any[]) => any) => (...args: any[]) => any {
    return (fn: (...args: any[]) => any) => {
      this._app.{{ method_name }}({{ meta_array }}, fn);
      return fn;
    };
  }
